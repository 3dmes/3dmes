version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: 3dmes-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-mesdb}
      POSTGRES_USER: ${POSTGRES_USER:-mesuser}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mespassword}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - mesnet
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-mesuser}"]
      interval: 10s
      timeout: 5s
      retries: 5

  3dmes:
    image: xhbtec/3dmes:${THREEMES_VERSION:-latest}
    container_name: 3dmes-app
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Production}
      ConnectionStrings__DefaultConnection: "Host=postgres;Port=5432;Database=${POSTGRES_DB:-mesdb};Username=${POSTGRES_USER:-mesuser};Password=${POSTGRES_PASSWORD:-mespassword}"
      ASPNETCORE_URLS: "http://+:80"
    networks:
      - mesnet
    user: "${DOCKER_USER_ID:-1000}:${DOCKER_GROUP_ID:-1000}"
    ports:
      - "${HTTP_PORT:-80}:80"
      - "${MQTT_PORT:-1883}:1883"
      - "${MQTTS_PORT:-8883}:8883"
    volumes:
      - data:/app/data
      - uploads:/app/uploads
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:80/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  mesnet:
    driver: bridge

volumes:
  postgres_data:
    driver: local
  data:
    driver: local
  uploads:
    driver: local

